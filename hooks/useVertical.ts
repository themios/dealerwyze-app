// Re-export from provider so consumers import from a consistent hooks/ path.
// Usage: import { useVertical, useLabels } from '@/hooks/useVertical'
export { useVertical, useLabels } from '@/components/providers/VerticalProvider'
