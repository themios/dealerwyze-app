---
phase: 07-listing-intelligence
plan: "04"
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - app/(app)/vehicles/new/page.tsx
  - app/(app)/vehicles/[id]/page.tsx
  - app/api/vehicles/[id]/route.ts
autonomous: false

must_haves:
  truths:
    - "New listing form (RE vertical) shows import buttons: 'Import from URL', 'Scan Photo', and 'Import via MLS#'"
    - "Agent pastes Zillow or Redfin URL — form fields pre-fill with extracted data; agent can edit before saving"
    - "Agent uploads a listing photo/flyer — AI-extracted fields populate the form; agent can edit before saving"
    - "Agent enters MLS# + address — RentCast-populated fields appear in the form; agent can edit before saving"
    - "Import buttons are only visible when org vertical is real_estate — dealer new-vehicle form is unchanged"
    - "Listing detail page shows performance metrics: days on market, showing count, price history"
    - "Listing detail page has a 'Generate CMA' button that calls /api/listings/[id]/cma and displays comparable sales"
    - "PATCH /api/vehicles/[id] tracks price changes: when price field changes, appends to price_change_log and increments price_change_count"
    - "All LIST-01 through LIST-07 requirements are exercisable end-to-end in the UI"
  artifacts:
    - path: "app/(app)/vehicles/new/page.tsx"
      provides: "RE import buttons and confirmation form prefill (LIST-01, LIST-02, LIST-03, LIST-07)"
    - path: "app/(app)/vehicles/[id]/page.tsx"
      provides: "Listing performance metrics section + CMA display (LIST-04, LIST-05)"
    - path: "app/api/vehicles/[id]/route.ts"
      provides: "Price change tracking on PATCH (LIST-04 data producer)"
  key_links:
    - from: "app/(app)/vehicles/new/page.tsx"
      to: "/api/listings/import-url"
      via: "fetch on URL paste submit"
      pattern: "api/listings/import-url"
    - from: "app/(app)/vehicles/new/page.tsx"
      to: "/api/listings/scan-photo"
      via: "fetch on photo file select"
      pattern: "api/listings/scan-photo"
    - from: "app/(app)/vehicles/[id]/page.tsx"
      to: "/api/listings/[id]/metrics"
      via: "fetch on page load (RE vertical only)"
      pattern: "api/listings.*metrics"
    - from: "app/(app)/vehicles/[id]/page.tsx"
      to: "/api/listings/[id]/cma"
      via: "fetch on 'Generate CMA' button click"
      pattern: "api/listings.*cma"
    - from: "app/api/vehicles/[id]/route.ts"
      to: "vehicles.price_change_log"
      via: "PATCH handler price diff check"
      pattern: "price_change_log|price_change_count"
---

<objective>
Wire the extraction API routes from plans 07-02 and 07-03 into the UI. The new listing page gets conditional import buttons for RE orgs with a confirmation-before-save flow. The listing detail page gains a performance metrics panel and CMA generation. The vehicles PATCH route is updated to track price changes. This plan delivers LIST-07 (confirmation preview), LIST-04 (metrics display), and LIST-05 (CMA display), and closes LIST-06 (org scoping verified end-to-end).

Purpose: Without this plan, the API routes exist but agents cannot access them. This plan makes the full import flows exercisable in the UI.
Output: Updated new listing form with import paths, listing detail with metrics and CMA, price change tracking in PATCH.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/tim/Applications/ApolloCRM/apollo-crm/.planning/phases/07-listing-intelligence/07-RESEARCH.md
@/home/tim/Applications/ApolloCRM/apollo-crm/.planning/phases/07-listing-intelligence/07-02-SUMMARY.md
@/home/tim/Applications/ApolloCRM/apollo-crm/.planning/phases/07-listing-intelligence/07-03-SUMMARY.md

# Files being modified
@/home/tim/Applications/ApolloCRM/apollo-crm/app/(app)/vehicles/new/page.tsx
@/home/tim/Applications/ApolloCRM/apollo-crm/app/(app)/vehicles/[id]/page.tsx
@/home/tim/Applications/ApolloCRM/apollo-crm/app/api/vehicles/[id]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RE import buttons + confirmation flow to new listing form (LIST-01, LIST-02, LIST-03, LIST-07)</name>
  <files>
    app/(app)/vehicles/new/page.tsx
  </files>
  <action>
    Read the current new/page.tsx carefully before editing. Make targeted additions only — do not restructure the existing dealer form flow.

    **Guard:** The import section is only rendered when the org's vertical is `real_estate`. Read vertical from the server component's profile/org context or pass it as a prop. Dealer orgs see no change to their form.

    **Import section placement:** Add a collapsible "Import Listing" panel above the main form fields (not replacing them). It should contain three import methods:

    **Method 1 — URL import (Zillow/Redfin):**
    - Text input: "Paste Zillow or Redfin listing URL"
    - "Import" button triggers `POST /api/listings/import-url` with `{ url }`
    - Show loading state: "Importing listing data..." (Apify takes up to 30s)
    - On success: merge returned fields into form state (prefill address_line1, city, state, zip, price, bedrooms, bathrooms, sqft, lot_size, year_built, property_type, hoa_monthly, photo_url, listing_url)
    - Show confirmation message: "Listing data imported. Review and edit the fields below before saving."
    - On error: show the error message from the API response (e.g., "Listing not found at that URL")
    - For Realtor.com: if user pastes a realtor.com URL, show inline guidance: "Realtor.com imports aren't supported — copy the listing description text and use 'Paste Listing Text' instead." (use domain detection client-side before calling API)

    **Method 2 — Photo scan:**
    - File input (accept: image/*) — "Upload listing photo or flyer"
    - On file select: read as base64, call `POST /api/listings/scan-photo` with `{ imageBase64, mimeType }`
    - Show loading: "Reading listing info from image..."
    - On success: merge non-null fields into form state
    - Show confirmation: "Fields extracted from image. Review and correct before saving."
    - On error: show API error message

    **Method 3 — MLS# import:**
    - Two inputs: "MLS Number" and a simplified address (line1, city, state, zip)
    - "Look Up" button calls `POST /api/listings/import-mls` with the fields
    - On success: if response includes a new `id` (record was created directly by the route), redirect to `/vehicles/{id}` instead of the form save path — the MLS import route already creates the record
    - On RentCast fields returned: show "Record created with RentCast data. Click to view your listing."
    - On error: show error message

    **Implementation notes:**
    - Use existing shadcn/ui components (Input, Button, Collapsible or Tabs) — do not install new UI packages
    - All fetch calls use the existing auth session (cookies are sent automatically from client components)
    - Import state is local (useState) — extracted fields merge into the existing form state object
    - Confirmation is implicit: extracted fields appear in the editable form. Agent must click "Save Listing" to persist. This satisfies LIST-07 (confirmation step before saving).
    - 'use client' at the top of this component (or extract import section into a new client component if the page is currently a server component)
    - Loading spinner and error states are required — no blank states
  </action>
  <verify>
    Manual verification (checkpoint below will confirm):
    - For a dealer org: import panel does not appear on /vehicles/new
    - For RE org: "Import Listing" panel is visible with all three methods
    - Pasting a Zillow URL and clicking Import shows loading, then prefills form fields
    - Uploading a listing flyer photo shows extracted fields in the form
    - Entering MLS# + address and clicking Look Up creates a record and redirects
    - Form can still be submitted manually without using any import method (no regressions)
  </verify>
  <done>RE orgs see three import methods on /vehicles/new. Dealer orgs see no change. Import results prefill the form (URL + photo) or redirect to new record (MLS). No form saves happen until the agent explicitly clicks Save.</done>
</task>

<task type="auto">
  <name>Task 2: Add metrics panel + CMA to listing detail, add price tracking to PATCH (LIST-04, LIST-05)</name>
  <files>
    app/(app)/vehicles/[id]/page.tsx
    app/api/vehicles/[id]/route.ts
  </files>
  <action>
    **app/(app)/vehicles/[id]/page.tsx — Listing Performance section:**

    Read the current detail page. For RE orgs only (gate on vertical), add a "Listing Performance" section to the listing detail view. Position it after the main listing info panel.

    Performance metrics panel (fetched from `GET /api/listings/[id]/metrics`):
    - "Days on Market": show as a number with "days" label
    - "Showing Count": show as a number
    - "Price Changes": if price_change_count > 0, show count and an expandable list of changes:
      - Each change: "Price reduced from $X to $Y on [date]" (format changed_at as locale date string)
    - Fetch on component mount; show skeleton while loading; show "No data available" if fetch fails

    CMA section:
    - "Comparable Market Analysis" header
    - "Generate CMA" button calls `GET /api/listings/[id]/cma`
    - Show loading: "Fetching comparable sales from RentCast..." (up to 15s)
    - On success: display structured CMA report:
      - Estimated value: `$price` (range: `$priceRangeLow – $priceRangeHigh`)
      - Comparable sales table: address, sale price, beds, baths, sqft, distance (mi), date sold
      - If `cached: true`: show "Data from [market_checked_at date] · Refresh" link that re-fetches
    - On error (503 for missing key): show "CMA unavailable — RentCast API key not configured"
    - On error (422 for incomplete address): show "Add a complete property address to generate a CMA"

    Both sections are only rendered when org vertical is `real_estate`. Read vertical server-side and pass as a prop to any client components.

    ---

    **app/api/vehicles/[id]/route.ts — Price change tracking (PATCH handler):**

    Read the existing PATCH handler carefully. Find where it applies field updates to the vehicles row. Add price change tracking:

    Before applying the update, check if `price` is in the incoming fields AND differs from the current vehicle price:
    ```typescript
    // Inside PATCH handler, after fetching current vehicle and before building update payload
    const incomingPrice = body.price  // from validated fields
    if (incomingPrice !== undefined && incomingPrice !== currentVehicle.price) {
      const changeEntry = {
        from: currentVehicle.price,
        to: incomingPrice,
        changed_at: new Date().toISOString(),
      }
      updatePayload.price_change_log = [
        ...(Array.isArray(currentVehicle.price_change_log) ? currentVehicle.price_change_log : []),
        changeEntry,
      ]
      updatePayload.price_change_count = (currentVehicle.price_change_count ?? 0) + 1
    }
    ```

    Scope this change to RE listings only (check `currentVehicle.make === 'RE'` or better, check `currentVehicle.address_line1 IS NOT NULL`). Do not track price changes on dealer vehicle records — this field is not meaningful for dealer inventory management.

    Note: `price_change_log` is a JSONB column added in migration 189. Confirm it exists in the EDITABLE_FIELDS allowlist or add it only to the internal update payload (not to EDITABLE_FIELDS since agents should not write to it directly — it is system-managed).
  </action>
  <verify>
    ```bash
    cd /home/tim/Applications/ApolloCRM/apollo-crm
    npm run build 2>&1 | grep -E "error|Error"
    npx eslint app/\(app\)/vehicles/ app/api/vehicles/\[id\]/route.ts --max-warnings=0
    # Expect: clean build, zero lint problems
    ```

    Functional checks:
    - Listing detail page renders without error for both dealer and RE orgs
    - For dealer org: no performance panel, no CMA section visible
    - For RE org: performance panel loads metrics; "Generate CMA" button triggers fetch
    - Updating a listing's price via PATCH increments price_change_count and appends to price_change_log
    - Dealer vehicle PATCH does not touch price_change_log or price_change_count
  </verify>
  <done>
    Listing detail page shows performance metrics and CMA for RE orgs only. PATCH handler tracks price changes on RE listings. Dealer detail and PATCH behavior unchanged. Build and lint pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 7 listing intelligence UI:
    - New listing form with URL import, photo scan, and MLS# import buttons (RE orgs only)
    - Listing detail performance panel (days on market, showing count, price history)
    - CMA generation and display
    - Price change tracking on PATCH
  </what-built>
  <how-to-verify>
    Using a RealtyWyze (RE vertical) org account:

    1. Navigate to /vehicles/new — confirm "Import Listing" panel is visible with three methods
    2. Test URL import: paste a Zillow listing URL → click Import → verify form fields prefill (address, price, beds, baths) → verify you can edit fields → click Save Listing → confirm record is created
    3. Test photo scan: upload a listing flyer image → verify extracted fields appear in form → edit one field → Save → confirm record is created
    4. Test MLS# import: enter a valid MLS# and property address → click Look Up → confirm redirect to new listing detail page
    5. On any listing detail page: verify the "Listing Performance" section shows days on market and showing count
    6. Click "Generate CMA" → verify comparable sales data appears within 15 seconds (requires RENTCAST_API_KEY set in env)
    7. Update a listing's price via the edit form → check the listing detail page shows the price change in the performance panel

    Using a DealerWyze (dealer) org account:
    8. Navigate to /vehicles/new — confirm NO import panel is visible; dealer form works normally
    9. Open a dealer vehicle detail — confirm no performance panel or CMA section

    If APIFY_API_TOKEN or RENTCAST_API_KEY are not yet configured, note which imports fail and confirm error messages are clear and actionable (not stack traces or technical errors).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found so they can be fixed before proceeding to the Phase 7 summary.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` — zero type errors
2. `npx eslint app/(app)/vehicles/ app/api/vehicles/[id]/route.ts --max-warnings=0` — zero problems
3. Dealer new vehicle form: no import buttons (vertical guard works)
4. RE new listing form: all three import methods visible and functional
5. Listing detail: performance + CMA only for RE orgs
6. Price tracking: RE PATCH appends to price_change_log; dealer PATCH unchanged
7. Human verification checkpoint passed
</verification>

<success_criteria>
- RE org agents can complete a full import from URL, photo, or MLS# through to a saved listing record
- Confirmation-before-save is enforced for URL and photo imports (LIST-07)
- Listing detail shows days on market, showing count, price history (LIST-04)
- CMA displays comparable sales from RentCast with 7-day cache (LIST-05)
- Dealer vertical unaffected — no UI or behavior changes visible to dealer orgs
- All release gates pass: lint zero, tests pass, build clean
</success_criteria>

<output>
After completion, create `/home/tim/Applications/ApolloCRM/apollo-crm/.planning/phases/07-listing-intelligence/07-04-SUMMARY.md`
</output>
