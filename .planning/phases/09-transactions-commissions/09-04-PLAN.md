---
phase: 09-transactions-commissions
plan: "04"
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - lib/vehicles/vehicleDetailSectionIds.ts
  - components/transactions/TransactionPanel.tsx
  - components/transactions/TransactionForm.tsx
  - components/transactions/TransactionStageBar.tsx
  - app/(app)/vehicles/[id]/page.tsx
autonomous: false

must_haves:
  truths:
    - "Transaction panel appears on listing detail page only when vertical = 'real_estate'"
    - "Agent can create a transaction (offer amount, offer date, buyer info, contingencies) from the listing detail panel"
    - "Agent can see current pipeline stage and advance it to the next valid stage"
    - "Agent can record closing date and final sale price from the panel (no commission calculation — that is broker-only)"
    - "Agent can log parties (buyer agent, seller agent, title company, lender) from the panel"
    - "Pipeline stage bar shows all 6 stages in order: Offer, Under Contract, Inspection, Appraisal, Closing, Closed"
    - "Panel does not appear for dealer vertical listings"
    - "Document attachment is handled by the existing VehicleDocuments component already on the page — no new doc UI needed"
  artifacts:
    - path: "components/transactions/TransactionPanel.tsx"
      provides: "Main transaction panel client component — fetches and renders transaction state"
    - path: "components/transactions/TransactionForm.tsx"
      provides: "Create/edit form for offer details, parties, and agent-recorded closing fields"
    - path: "components/transactions/TransactionStageBar.tsx"
      provides: "Visual pipeline stage indicator with advance-stage button — all 6 stages"
    - path: "lib/vehicles/vehicleDetailSectionIds.ts"
      provides: "transactions section ID added to constant"
    - path: "app/(app)/vehicles/[id]/page.tsx"
      provides: "Transaction nav entry and panel wired in for RE orgs"
  key_links:
    - from: "components/transactions/TransactionPanel.tsx"
      to: "/api/transactions?vehicle_id=X"
      via: "fetch on mount and after mutations"
      pattern: "fetch.*api/transactions"
    - from: "app/(app)/vehicles/[id]/page.tsx"
      to: "components/transactions/TransactionPanel.tsx"
      via: "isRe guard in nav sections and panel render"
      pattern: "isRe.*TransactionPanel|TransactionPanel.*isRe"
---

<objective>
Build the TransactionPanel UI and wire it into the listing detail page as an RE-only section (TXN-01, TXN-02, TXN-03, TXN-04).

Purpose: Agents need to create transactions, track pipeline stages, record closing details, and log parties without leaving the listing detail view.
Output: A working TransactionPanel component wired into the listing detail page behind the isRe guard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-transactions-commissions/09-RESEARCH.md
@.planning/phases/09-transactions-commissions/09-02-SUMMARY.md
@lib/vehicles/vehicleDetailSectionIds.ts
@app/(app)/vehicles/[id]/page.tsx
@components/vehicle/VehicleDocuments.tsx
@lib/transactions/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TransactionPanel, TransactionForm, TransactionStageBar components</name>
  <files>components/transactions/TransactionPanel.tsx, components/transactions/TransactionForm.tsx, components/transactions/TransactionStageBar.tsx</files>
  <action>
    Create `components/transactions/TransactionStageBar.tsx` — client component:
    - Props: `currentStage: PipelineStatus`, `onAdvance: (to: PipelineStatus) => void`, `isLoading: boolean`, `isAdmin: boolean`
    - Render a horizontal stepper showing all 6 pipeline stages in order:
      Offer → Under Contract → Inspection → Appraisal → Closing → Closed
    - Fallen_through appears as a side badge/button if stage is not closed
    - Active stage highlighted. Completed stages dimmed. Future stages grey.
    - Show "Advance to [next stage]" button only when VALID_TRANSITIONS[currentStage] has a non-fallen_through option
    - Show "Mark Fallen Through" button if stage is not closed and not already fallen_through
    - When disabled stages are clicked, no action. Use Tailwind for styling — follow shadcn/ui patterns already in codebase.

    Create `components/transactions/TransactionForm.tsx` — client component:
    - Props: `vehicleId: string`, `transaction?: Transaction | null`, `onSave: (t: Transaction) => void`, `onCancel: () => void`
    - When transaction is null: show "Create Transaction" form. When transaction is set: show "Edit" form.
    - Fields: Offer Amount (number input), Offer Date (date input), Inspection Deadline (date input), Commission % (number, default 3), Co-Broke % (number, default 3), Contingencies (comma-separated text that builds array), Notes (textarea)
    - Closing Details section (shown to all agents — no role gate): Closing Date (date input), Final Sale Price (number input). Label with helper text: "Record the closing date and final price. Your broker will confirm close to finalize commissions."
    - Parties section (collapsible): Buyer Agent (text), Seller Agent (text), Title Company (text), Lender (text)
    - Submit calls POST /api/transactions or PATCH /api/transactions/[id] depending on mode
    - Show inline error message on API failure (never expose raw error). Show loading state on submit.
    - Use react-hook-form or simple controlled state — whichever pattern is already prevalent in the codebase (check components/vehicle/ for pattern)

    Create `components/transactions/TransactionPanel.tsx` — client component:
    - Props: `vehicleId: string`, `isAdmin: boolean`
    - On mount: fetch GET /api/transactions?vehicle_id=vehicleId. Store `transactions` array in state.
    - If no transactions: show "No transaction yet" empty state with "Create Transaction" button that opens TransactionForm.
    - If transaction(s) exist: show the most recent active transaction (not fallen_through) prominently. List others collapsed.
    - Show TransactionStageBar for the active transaction.
    - When advance-stage is clicked: PATCH /api/transactions/[id] with new pipeline_status. Refresh data after success.
    - Show key details: offer amount, offer date, inspection deadline, parties (if set), commission % if set.
    - Show closing date and final sale price fields (editable by all — agents record these; broker confirms via separate Close button in Plan 09-05).
    - Show "Edit" button that opens TransactionForm pre-filled with current transaction data.
    - Note at bottom of panel: "Transaction documents are in the Private Files section above." (pointing users to existing VehicleDocuments — no new doc UI needed per research).
    - Handle loading and error states. No blank panels.
  </action>
  <verify>
    TypeScript: `npx tsc --noEmit` passes.
    Lint: `npx eslint components/transactions --max-warnings=0` passes.
    Component renders without runtime errors in dev mode when loaded on a listing detail page.
    TransactionStageBar renders all 6 stages (Offer, Under Contract, Inspection, Appraisal, Closing, Closed) in order.
  </verify>
  <done>Three components exist, compile, and render. TransactionPanel fetches from correct API. TransactionForm posts to correct endpoint and includes closing date/final sale price fields for agents. StageBar renders all 6 pipeline stages and advance button.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TransactionPanel into listing detail page</name>
  <files>lib/vehicles/vehicleDetailSectionIds.ts, app/(app)/vehicles/[id]/page.tsx</files>
  <action>
    In `lib/vehicles/vehicleDetailSectionIds.ts`:
    Add `transactions: 'vehicle-detail-transactions'` to the VEHICLE_DETAIL_SECTION_IDS object.

    In `app/(app)/vehicles/[id]/page.tsx`:
    1. Import `TransactionPanel` from `@/components/transactions/TransactionPanel`.
    2. In the `navSections` building block, add a new entry after the Showings section (or after 'Closing') — but only if `isRe`:
       ```typescript
       if (isRe) {
         navSections.push({ id: VEHICLE_DETAIL_SECTION_IDS.transactions, label: 'Transaction' })
       }
       ```
    3. In the panel rendering section, add:
       ```tsx
       {isRe && (
         <section id={VEHICLE_DETAIL_SECTION_IDS.transactions} ...>
           <SectionHeading>Transaction</SectionHeading>
           <TransactionPanel vehicleId={id} isAdmin={isAdmin} />
         </section>
       )}
       ```
    4. Do not alter any existing dealer-vertical panels or sections. The `isRe` guard ensures zero impact on dealer listings.
    5. Do not change section order for dealer vertical — only RE vertical gets the new section.

    Use the same SectionHeading component and section wrapper className pattern used for other panels on the page.
  </action>
  <verify>
    Build passes: `npm run build` has no type errors on vehicles/[id]/page.tsx.
    Navigating to an RE listing detail page: "Transaction" appears in the jump nav and the TransactionPanel renders in the page body.
    Navigating to a dealer listing detail page: no "Transaction" nav item, no TransactionPanel rendered.
  </verify>
  <done>transactions section ID added to constant. Listing detail page renders TransactionPanel for RE orgs only. Dealer vertical unaffected.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no errors
- `npm run build` — clean build
- RE listing detail page: Transaction section visible in nav; TransactionPanel renders; can create a transaction; can advance stage through all 6 stages
- TransactionForm shows closing date and final sale price fields to all agents
- Dealer listing detail page: Transaction section absent
</verification>

<success_criteria>
TransactionPanel is live on RE listing detail pages. Agent can create a transaction, view pipeline stage (all 6 stages), advance stage, record closing date and final sale price, and log parties. Dealer vertical unaffected. TXN-01, TXN-02, TXN-03, TXN-04 all have UI coverage.
</success_criteria>

<checkpoint type="checkpoint:human-verify" gate="blocking">
  <what-built>TransactionPanel on listing detail page — create transaction, 6-stage pipeline, parties log, closing date/price fields for agents</what-built>
  <how-to-verify>
    1. Open an RE listing detail page in the browser
    2. Confirm "Transaction" appears in the page jump nav
    3. Click "Create Transaction" — form opens with offer amount, dates, contingencies, parties fields, and closing date/final sale price fields
    4. Fill in offer amount and offer date, submit — transaction appears with stage bar showing "Offer" active
    5. Confirm stage bar shows all 6 stages in order: Offer, Under Contract, Inspection, Appraisal, Closing, Closed
    6. Click "Advance to Under Contract" — stage bar updates, API call succeeds
    7. Advance through Inspection → Appraisal → Closing to verify all transitions work
    8. Edit the transaction — enter a closing date and final sale price, save — confirm these are stored
    9. Open a dealer listing detail page — confirm Transaction section is absent
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</checkpoint>

<output>
After completion, create `.planning/phases/09-transactions-commissions/09-04-SUMMARY.md`
</output>
