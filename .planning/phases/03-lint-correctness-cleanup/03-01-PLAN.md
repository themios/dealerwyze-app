---
phase: 03-lint-correctness-cleanup
plan: 01
type: execute
wave: 1
depends_on:
  - phase-02-active
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Phase 3 is executed by workstream and verified after each batch"
    - "No lint rule is disabled to manufacture a pass"
    - "Security-sensitive code paths are prioritized ahead of hygiene-only cleanup"
    - "Build and tests remain green throughout the phase"
  artifacts:
    - path: ".planning/lint-baseline.md"
      provides: "current quantitative baseline for Phase 3"
    - path: ".planning/phases/03-lint-correctness-cleanup/03-RESEARCH.md"
      provides: "risk-based rationale for workstream order"
---

<objective>
Execute Phase 3 cleanly and measurably. Maintain the current zero-problem source lint baseline while using risk-prioritized workstreams to keep the codebase stable as Phase 2 changes are integrated.

Output: a phased remediation sequence that can be executed in batches with clear stop points and verification gates.
</objective>

<workstreams>

## Workstream A — React Correctness

Priority: highest

Rules:
- `react-hooks/rules-of-hooks`
- `react-hooks/set-state-in-effect`
- `react-hooks/purity`

Reason:
- directly impacts runtime behavior, hydration stability, and component correctness

Representative hotspots from the current lint run:
- `components/today/AppointmentRequestCard.tsx`
- `hooks/useOrgSettings.ts`
- multiple `app/(app)/admin/*` pages
- `components/today/ResponseTimeWidget.tsx`
- `components/vehicle/MechanicWorksheetCard.tsx`
- `components/vehicle/ReconSection.tsx`

Verification after batch:
- rerun eslint on touched files
- rerun `npm run build`

## Workstream B — Sensitive Type Safety

Priority: high

Rules:
- `@typescript-eslint/no-explicit-any`

Scope first:
- `app/api/` payment/auth/webhook/public routes
- `lib/` helpers used by payments, pricing, and integrations

Reason:
- improves security reviewability and refactor safety in the most critical paths

Verification after batch:
- rerun eslint on touched files
- rerun `npm test`

## Workstream C — Framework Correctness

Priority: medium

Rules:
- `@next/next/no-html-link-for-pages`
- `@next/next/no-img-element`

Reason:
- operational correctness and framework consistency

Verification after batch:
- rerun eslint on touched files
- spot-check routing/image behavior where changed

## Workstream D — Hygiene Reduction

Priority: last

Rules:
- `@typescript-eslint/no-unused-vars`
- `react/no-unescaped-entities`
- other low-risk residual warnings/errors

Reason:
- high volume, lower architectural risk

Verification after batch:
- full lint
- build
- tests

</workstreams>

<execution_order>
1. Workstream A
2. Workstream B
3. Workstream C
4. Workstream D
5. Final full-project verification
</execution_order>

<verification>
At the end of each workstream:

```bash
npx eslint <touched files> --max-warnings=0
```

At the end of Phase 3:

```bash
npx eslint app components hooks lib remotion next.config.ts proxy.ts --max-warnings=0
npm run build
npm test
```
</verification>

<success_criteria>
- full source lint command exits 0
- no rule suppressions added for Phase 3 correctness rules without explicit justification
- build passes
- tests pass
- planning docs updated with final counts and completion status
</success_criteria>
